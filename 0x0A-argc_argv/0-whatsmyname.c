#include<stdio.h>

/**
 * main - entry point
 * @argc:counts arguments
 * @argv:array of charcters
 *
 * Return: always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
